/** this is the program that solves the merging of two sorted linked List
the problem can be found on this link : https://leetcode.com/problems/merge-two-sorted-lists/
*/

/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     struct ListNode *next;
 * };
 */
/*
struct ListNode* mergeTwoLists(struct ListNode* list1, struct ListNode* list2){
printf("is this working ? ");
}
*/
int main () {
int a,b; 
a= 1 ; 
b = 2 ; 
printf("%d",&a);

}